{
  "name": "hacs-actions",
  "private": true,
  "description": "Custom actions for the HACS org",
  "scripts": {
    "build": "tsc",
    "push": "rm -R lib && npm install && npm run build && npm prune --production && git add . && git commit -m 'update' && git push"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hacs/actions.git"
  },
  "author": "Joakim Sørensen (@ludeeus)",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.0.0",
    "@actions/github": "^1.0.0",
    "@octokit/rest": "^16.33.1"
  },
  "devDependencies": {
    "@types/node": "^12.11.1",
    "typescript": "^3.5.1"
  }
}
