name: "Test action"
on: ["push"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - run: npm ci
    - run: npm run build
    - uses: ./
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
